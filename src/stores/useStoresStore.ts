import { defineStore } from 'pinia'

/**
 * @method useStoresStore
 * @summary Store managing CRUD operations for shops — list, create, update, delete
 */
export const useStoresStore = defineStore('stores', () => {
  return {}
})
