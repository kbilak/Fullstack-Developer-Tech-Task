import { defineStore } from 'pinia'

/**
 * @method useStatisticsStore
 * @summary Store managing daily visit statistics for a selected shop within a date range
 */
export const useStatisticsStore = defineStore('statistics', () => {
  return {}
})
