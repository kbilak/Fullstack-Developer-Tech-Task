import { defineStore } from 'pinia'

// TODO: implement statistics state (daily visit counts, date range, selected store)
export const useStatisticsStore = defineStore('statistics', () => {
  return {}
})
